Raw GitHub Actions and Terraform text outputs originally contained trailing
spaces generated by their table and log renderers.

Before committing the evidence, trailing spaces and tabs were removed from
text and log files so that Git whitespace validation could pass.

No commands, timestamps, statuses, resource identifiers, workflow results,
or semantic log content were changed.
